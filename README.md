# NativeScript HTTP GET Example

This is a simple NativeScript HTTP GET example that outputs the response from a HTTP GET request to the console.

# How to Use

Restore the npm packages:

> npm install

Run as iOS or Android

> tns run ios

or 

> tns run android